#include "espaura.h"

struct wifi {
    void init() {
        // Initialize WiFi (if applicable)
        // This function is called once at the beginning of the program
        // Add your WiFi initialization code here
        // For example, you can connect to a WiFi network or set up a hotspot
        Serial.println("WiFi Initialized"); // Print a message to the serial monitor
    }
};